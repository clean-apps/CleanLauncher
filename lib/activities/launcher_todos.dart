import 'package:CleanLauncher/widgets/launcher/todo_list.dart';
import 'package:flutter/material.dart';

class LauncherTodos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: TodoListWidget(),
    );
  }
}
